# OCR CSV Batch API

A FastAPI-powered backend to scan title PDFs, extract structured CSVs, fill missing fields using GPT-4o, and log usage into Supabase.

## Features
- OCR PDF using Tesseract
- Structured CSV output with defined headers
- GPT-4o Assistant fills in missing fields
- Supabase usage tracking
